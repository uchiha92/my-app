import { ImageMissingDirective } from './image-missing.directive';

describe('ImageMissingDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageMissingDirective();
    expect(directive).toBeTruthy();
  });
});
