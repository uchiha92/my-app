import { RegisterFormGroup } from './register-form-group';

describe('SubmitFormGroup', () => {
  it('should create an instance', () => {
    expect(new RegisterFormGroup({})).toBeTruthy();
  });
});
